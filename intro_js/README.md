# Introducción a JavaScript

Introducción a JS donde se analizaron los tipos de datos en js como:
- Datos Primitivos (Number, string, boolean, undefined, Bigint, Null y Symbol)
- Objetos/Referencias (Arrays, objetos)

Por ultimo se vieron algunas palabras reservadas del lenguaje y la sintaxis de variables como:
- let
- const
- var
